/**
 * Must match `CLUSTER_ZOOM_THRESHOLD` in
 * `src/modules/properties/repositories/property-read.repository.ts`.
 * At this zoom the map API returns individual property pins (no clusters).
 */
export const MAP_UNCLUSTERED_ZOOM = 15;
